#include <iostream>
using namespace std;
int main() {


	int n;

	cout << "Nhap vao so chinh phuong: ";
	cout << "\nNhap vao n:";
	cin >> n;

	float canBacHai = sqrt(n);
	short bay = 0;

	for (int i = 1; i <= canBacHai; i++)
		if (i * i == ) {
			bay - 1;
			break;
		}
	if (bay)
		cout << n << " la so chinh phuong ";
	else
		cout << n << " Khong phai so chinh phuong ";
	cout << "Day vua nhap a:";
	for (short i = 0; i < n; i++)
		cout << a[i] << " ";
	short S = a[0];
cout << a[n - 1];
cout << "\nTong:" << S;
}